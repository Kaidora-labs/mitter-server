meta {
  name: Initiate
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/initiate
  body: json
  auth: inherit
}

body:json {
  {
    "emailAddress": "{{EMAIL_ADDRESS}}",
    "password": "{{PASSWORD}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
