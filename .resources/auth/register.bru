meta {
  name: register
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "emailAddress": "{{EMAIL_ADDRESS}}",
    "password": "{{PASSWORD}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
