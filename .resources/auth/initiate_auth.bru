meta {
  name: Initiate Auth
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/initiate
  body: json
  auth: inherit
}

body:json {
  {
    "email_address": "{{EMAIL_ADDRESS}}",
    "password": "{{PASSWORD}}",
    "otp": ""
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
