meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "first_name": "Simon",
    "last_name": "Samuel",
    "phone_number": "{{PHONE_NUMBER}}",
    "email_address": "{{EMAIL_ADDRESS}}",
    "wallet_address": "{{WALLET_ADDRESS}}",
    "password": "{{PASSWORD}}",
    "role": "USER"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
