meta {
  name: post user
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "firstName": "Test",
    "lastName": "Test",
    "emailAddress": "{{EMAIL_ADDRESS}}",
    "phoneNumber": "{{PHONE_NUMBER}}",
    "password": "{{PASSWORD}}",
    "walletAddress": null,
    "business_id": null,
    "role": "Proprietor"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
